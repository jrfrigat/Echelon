# 011 — Аудит перед релизом

Статус: выполнен 2026-08-01, ветка `fix/audit-remediation`. Сборка: 0 ошибок, 0 предупреждений.
Тесты: 521 зелёный (было 493). Все 29 миграций MsSql накатаны начисто на живой SQL Server 2022,
`has-pending-model-changes` чист на обоих провайдерах и на архивном контексте.

Полный проход по коду, XML-документации, комментариям и документации на обоих языках. Ниже —
**что найдено и исправлено** и **что осталось решать**. Второй список важнее: это то, что
сознательно не тронуто, и причина.

---

## 1. Исправлено: логика

### 1.1 Даты GitLab приезжали с `Kind=Local` (критично для PostgreSQL)

`GitLabProvider` разбирал `created_at`/`merged_at` в `DateTime`. GitLab отдаёт ISO-8601 со
смещением, а `System.Text.Json` в этом случае строит `DateTime` с `Kind=Local`. Это ровно тот
дефект, о котором предупреждает `CLAUDE.md`:

- **SQL Server** проглатывает такое значение молча и хранит неверный момент времени, если хост не
  на UTC;
- **PostgreSQL** отвергает его: Npgsql пишет в `timestamptz` только `Kind=Utc` и бросает на `Local`.

То есть баг невидим, пока не запустят вторую БД. Адаптер Яндекс.Трекера уже делал правильно —
разбирал через `DateTimeOffset` и отдавал `.UtcDateTime`, — а GitLab нет; расхождение между двумя
адаптерами и было подсказкой.

Исправлено: `DateTimeOffset` в DTO, `.UtcDateTime` на выходе. Контракт «все даты — UTC, конвертация
это работа адаптера» теперь записан на самом порту `VcsMergeRequest`, чтобы следующий адаптер не
повторил.

### 1.2 Реконсиляция задач не доходила дальше первой страницы

`TaskReconciliationService` каждый проход брал `.OrderBy(t => t.Id).Take(MaxTasksPerRun)` — то есть
**одни и те же** первые N задач. При числе открытых задач больше лимита (по умолчанию 500) все
остальные не сверялись **никогда**. При этом лог утверждал обратное: «the remainder waits for the
next pass».

Цена дефекта прямая: связи задач — единственное, что задаёт порядок плана, а трекеры не шлют
событие при добавлении или удалении связи. Именно этот проход и есть страховка; для задач за
лимитом её не было.

Исправлено: курсор по `Id`, проход продолжается с места остановки и заворачивается по кругу.
Сообщение в логе больше не обещает того, чего не делает. Покрыто тестами
(`TaskReconciliationRotationTests`) — проверено, что три из четырёх падают на старом поведении.

### 1.3 Повтор имени ветки в одном снапшоте отравлял очередь

`(RepositoryId, Name)` в `RepositoryBranch` уникален. `BranchesObservedConsumer` вставлял каждую
наблюдённую ветку без дедупликации, поэтому снапшот с одинаковым именем дважды ронял
`SaveChangesAsync`. При at-least-once доставке это не разовая ошибка, а сообщение, которое
переотправляется и падает вечно, унося с собой все последующие обновления веток этого репозитория.

Исправлено: дедупликация входящего снапшота (побеждает последняя запись, как и в upsert ниже).
Консьюмер заодно получил первое тестовое покрытие (`BranchesObservedConsumerTests`, 7 тестов).

### 1.4 `NullReferenceException` при запуске выкатки по устаревшему плану

`RolloutService.LaunchAsync` брал `mr.TaskId!.Value`. `MergeRequest.TaskId` — `SetNull`, поэтому
архивация задачи обнуляет связь, а план, построенный до этого, всё ещё ссылается на строку. Обе
колонки шага не-nullable, так что запуск падал бы `NullReferenceException` и 500 без объяснения.

Исправлено: понятная доменная ошибка с единственной инструкцией, которая чинит («пересчитайте план»).

### 1.5 Ручной опрос терял счётчик веток

`POST /api/vcs-connections/{id}/poll` возвращал `emitted` и `failures`, но выбрасывал
`VcsPollResult.Branches`. Проход может состоять целиком из веток — репозиторий, где работа ещё не
дошла до ревью, не отдаёт ни одного MR, но держит родительскую задачу. Оператор видел
`emitted: 0` и читал это как «ничего не произошло».

### 1.6 Регистр имён веток и путей репозиториев на SQL Server

Найдено как теоретический риск, **подтверждено на живом SQL Server 2022** и после этого исправлено.

`IX_RepositoryBranch_RepositoryId_Name` уникален, коллация нигде не задавалась, поэтому колонка
наследовала умолчание инстанса — на проверочной БД это `Cyrillic_General_CI_AS`,
регистронезависимая. Имена веток в Git регистрозависимы. Воспроизведение:

```
INSERT ... RepositoryBranches ... 'feature/Login'  -> ок
INSERT ... RepositoryBranches ... 'feature/login'  -> Msg 2601, duplicate key
```

Это происходит внутри консьюмера, то есть отказ не разовый: сообщение переотправляется и падает
вечно, унося все последующие обновления веток этого репозитория. На PostgreSQL, где коллация по
умолчанию регистрозависима, того же кода достаточно и он работает — классическая «невидимо до
второй БД» ошибка.

То же рассуждение верно для `Repositories.ExternalId`: путь `group/project` в GitLab
регистрозависим, а `IX_Repository_ConnectionId_ExternalId` уникален.

Исправлено: явная коллация `Latin1_General_100_BIN2` на обеих колонках, только для SQL Server —
третье (и пока последнее) расхождение провайдеров в `ProviderSpecificMapping`. `BIN2`, а не
`_CS_AS`: это непрозрачные идентификаторы, сравниваемые на равенство, а не сортируемые для человека.
Закреплено тестами модели на обоих провайдерах: на SQL Server коллация обязана быть, на PostgreSQL
её обязано не быть (имя не разрешилось бы).

### 1.7 Выведенные статусы можно было выставить руками

`PATCH /api/merge-requests/{id}/status` принимал `Reviewed` и `ReadyForDeploy`. Значение при этом
ничего не решает — готовность считается правилом окружения, — но взводит `IsStatusManual`, который
запрещает наблюдению переопределить статус. То есть MR залипал в статусе, на который не может
среагировать ни система, ни оператор.

Исправлено: `MergeRequestStatusResolver.IsRetired` и отказ на входе с указанием, что делать вместо
этого (закрепить готовность для окружения). Сами члены enum остаются — колонка хранит их имена, и
удаление сделало бы старые строки нечитаемыми.

### 1.8 `NU1900` ломал сборку вопреки собственному комментарию

`Directory.Build.props` утверждал: *«That is a statement about the network, not the code, so it
stays a warning rather than a fatal error — a restore must not fail because a feed is briefly
unreachable»*. Но `TreatWarningsAsErrors=true` действует безусловно и повышал `NU1900` до ошибки.

Найдено не чтением: во время самого аудита nuget.org стал недоступен, и сборка легла с девятью
`NU1900` и без выхода. Исправлено через `WarningsNotAsErrors` — **не** `NoWarn`: предупреждение
обязано остаться видимым, потому что именно оно говорит, что аудит уязвимостей не отработал.

---

## 2. Исправлено: документация и комментарии

### 2.1 Пять несуществующих ключей конфигурации

`docs/{en,ru}/configuration.md` документировали ключи, которых **нет в коде**. Оператор, выставив
любой из них, получал тишину:

| Документировалось | Фактически |
|---|---|
| `Archiving__CutoffDays` | `Archiving__ArchiveAfterDays` |
| `Archiving__BatchSize` | `Archiving__MrBatchSize` + `Archiving__TaskBatchSize` |
| `Archiving__RunIntervalMinutes` | `Archiving__RunAtUtcHour` (час, а не интервал) |
| `TaskReconciliation__RunIntervalMinutes` | `TaskReconciliation__IntervalMinutes` |
| `TaskReconciliation__BatchSize` (по умолч. 100) | `TaskReconciliation__MaxTasksPerRun` (по умолч. 500) |

Не документировались вовсе: `Archiving__StatusJournalRetentionDays` (ретенция журналов — то, что
управляет ростом самой большой таблицы) и `Archiving__RunAtUtcHour`.

Плюс прямая неправда: «Нет leader election — архивация запускается во всех репликах». Архивация
работает под lease с 2026-07.

### 2.2 `architecture.md` расходился с кодом в четырёх местах

- контракты сообщений объявлены живущими в `Providers.Abstractions.Ingestion` — они в
  `Application.Contracts.Messages`; в `Providers.Abstractions.Ingestion` лежит парсерная сторона шва;
- волны и шаги описаны как «репозитории» — и то и другое считается по **merge request'ам**;
- гейт готовности описан как удерживающий шаг — он отрабатывает один раз на запуске и отклоняет
  **запуск целиком** (повторной проверки при диспатче нет вообще);
- в readiness-пробе перечислен RabbitMQ, которого там нет: проверяются рабочая БД, архивная БД и
  координационный кэш.

### 2.3 Отслеживание веток не было описано нигде, где его будет искать оператор

Незавершённая работа блокирует запуск выкатки с сообщением «Unfinished work blocks this rollout»,
и это поведение не упоминалось ни в user-guide, ни в operations. Добавлен раздел на обоих языках:
что именно считается незавершённой работой, почему обычная исходная ветка MR из плана не блокирует
ничего, и три способа разблокироваться.

### 2.4 Комментарии, пережившие то, что описывали

- пять ссылок вида `README §5.3`, `§6.2`, `§3.1`, `§9.1`, `§11.3` — после реструктуризации README
  разделов с такой нумерацией не существует;
- три комментария описывали promotion по ready-for-deploy лейблу, который был снят миграцией
  `RetireReadyForDeployLabel`;
- `RepositoryDependency` утверждал, что стеки «удаляются в фазе очистки» — они уже удалены;
- `BrokerUnavailableExceptionHandler` ссылался на обещание README о буфере в памяти; README теперь
  фиксирует обратное — буферизация рассмотрена и отвергнута.

Неверный комментарий дороже отсутствующего, поэтому это правилось наравне с логикой.
**Избыточных** комментариев (пересказывающих код) и закомментированного кода поиск не выявил —
дисциплина комментариев в проекте выдержана, они объясняют «почему».

### 2.5 XML-документация

Задокументированы: порт `IVcsService`, `MergeRequestStatus`, `StackDependencyType`, контракты
сообщений, `Paging`, `TokenProtector`, `UserIdentifier`, `Permissions`, `PermissionRequirement`,
`PermissionClaimsTransformation`, `ArchiveOptions`, `TelemetryOptions`, `TaskReconciliationOptions`
и все ранее недокументированные эндпоинты четырёх контроллеров.

Отдельно: у `MergeRequestStatus.Reviewed` и `ReadyForDeploy` теперь написано, что они **выведены из
употребления**, и почему их нельзя просто удалить — enum хранится строкой, и удаление члена сделает
старые строки нечитаемыми.

---

## 3. Не исправлено — требует решения

> Два пункта, стоявшие здесь при первом проходе — регистр имён веток и выставление выведенных
> статусов — с тех пор решены и переехали в п. 1.6 и 1.7. Остальные требуют либо продуктового
> решения, либо объёма, не относящегося к аудиту.

### 3.1 Регистр в прочих natural keys (осознанно оставлено регистронезависимым)

Коллация правилась ровно на двух колонках — `RepositoryBranches.Name` и `Repositories.ExternalId`, —
там, где регистр значим у источника. Остальные natural keys оставлены на умолчании инстанса
намеренно:

- `TaskItem.ExternalId` — ключи задач трекеров регистронезависимы на практике (`PROJ-1` и `proj-1`
  один и тот же тикет), и совпадение по регистру здесь скорее помогает;
- `VcsConnection.Name` / `TrackerConnection.Name` — имена, которые вводит оператор; требовать
  точного регистра значило бы ловить его на опечатке в регистре.

Это решение, а не пропуск: если понадобится обратное, менять надо ту же точку
(`ProviderSpecificMapping`) и так же мигрировать оба провайдера. Стоит перечитать, если появится
трекер с регистрозависимыми ключами.

### 3.2 `GetActivePlanAsync` показывает сохранённую версию с пересчитанным содержимым

Метод читает `RolloutPlan` из БД ради метаданных (`Version`, `CreatedAt`, авторство), но узлы,
волны и конфликты **пересчитывает из текущего атласа**. Если атлас изменился после того, как план
был сохранён, API отдаёт версию X с содержимым, которого в версии X не было. `ContentHash`
сохранённой строки при этом не сверяется.

На запуск это не влияет — `LaunchAsync` строит волны сам, из `PlanItem` сохранённого плана. Влияет
на то, что видит оператор перед запуском. В коде помечено как «override replay — follow-up within
P2»; здесь фиксируется, что расхождение видимое, а не только теоретическое.

### 3.3 `RolloutPlan.Version` — метка с точностью до секунды

`now.ToString("yyyyMMddHHmmss")`. Два пересчёта в одну секунду дадут одинаковую метку. Уникального
индекса на `Version` нет, поэтому это не падение, а неоднозначность в истории. Каждое событие
приёма перестраивает все активные планы, так что совпадение в пределах секунды вполне достижимо.

### 3.4 Архив не может забрать задачи, попавшие в историю выкаток

`ArchiveRunner` пропускает задачу, если на неё ссылается `PlanTaskNode`, `RolloutPlan`, `Rollout`
или `RolloutStep` (все связи `Restrict`). Ничто не удаляет историю выкаток, поэтому любая задача,
которую хоть раз выкатывали, не архивируется **никогда**. В коде это отмечено как follow-up;
фиксируется здесь как реальное ограничение ретенции, а не как заметка на полях.

### 3.5 351 публичный член без XML-комментария

Осталось преимущественно в `Pwa/Models/ApiModels.cs` (193), `Pwa/Services/ApiService.cs` (39),
`AppDbContext` (32, это `DbSet`-ы) и `ArchivedEntities.cs` (17). Это ровно та категория, которую
`Directory.Build.props` сознательно выводит из-под правила: имена уже говорят всё, а «Gets or sets
the Id» обесценивает комментарии как жанр. Осознанно не трогалось.

### 3.6 Ссылки `README §N` в `001-current-state.md`

Оставлены намеренно: 001 — датированный отчёт об аудите, он фиксирует, что README говорил тогда.
Переписать их значило бы подделать исторический документ.

---

## 4. Что проверено и оказалось в порядке

Чтобы список выше читался правильно, стоит сказать и обратное.

- **Алгоритм планирования** (`ReleasePlanGraph`, `PlanClosureBuilder`) — разобран построчно:
  обход циклов, реконструкция цикла по родителям, границы Kahn, детерминизм по `rank`, защита от
  молча усечённого плана. Дефектов не найдено.
- **Правила готовности** (`ReadinessResolver`, `ReadinessSignals`, `LabelSet`) — канонизация
  сигналов правила и сигналов MR идёт через один и тот же путь, сравнение ординарное, пустое
  правило не допускает никого. Согласовано.
- **Inbox дедупликации** — отметка ставится после успеха обработчика, дубль отличается от
  транзиентного сбоя проверкой наличия строки. Корректно.
- **Координатор выкаток** — заявки, восстановление зависших шагов, различение «poll упал» и «деплой
  провалился», сброс `ChangeTracker` между выкатками одного прохода. Возражений нет.
- **Пустые файлы** — `scripts/clean-empty-files.sh` прогонялся перед каждым коммитом; за сессию
  поймал 4 штуки, все — следствие неэкранированного `>` в моих же shell-командах. Ровно тот
  сценарий, ради которого скрипт написан. В коммиты не попали.
- **Синхронность `docs/ru` и `docs/en`** — совпадает по числу заголовков во всех семи парах;
  битых внутренних ссылок и ссылок на несуществующие пути нет.
- **Миграции на живой БД** — все 29 накатились начисто на SQL Server 2022 из пустой базы, включая
  новую про коллацию (она меняет колонку, в которой уже лежали строки). `Down` у неё есть и
  симметричен. Оба провайдера и архивный контекст не имеют расхождений модели и миграций.

## 5. Чем это проверялось

Стоит записать, потому что часть проверок нельзя воспроизвести чтением:

| Что | Как проверено |
|---|---|
| Регистр веток (1.6) | Два `INSERT` на живом SQL Server 2022 — `Msg 2601` до, оба имени сосуществуют после; точный дубль по-прежнему отвергается |
| Ротация реконсиляции (1.2) | Тесты прогнаны против старого поведения — падают 3 из 4 |
| Даты GitLab (1.1) | Тест прогнан против старой привязки — `Expected: Utc, Actual: Local` |
| Миграции | `database update` начисто на живой БД + `has-pending-model-changes` на трёх контекстах |
| Пустые файлы | `scripts/clean-empty-files.sh` перед каждым коммитом |

Не проверено на живой системе: сам запуск приложения, PostgreSQL (сервера нет), реальные вызовы
GitLab и RabbitMQ. Это ограничение из README остаётся в силе.
