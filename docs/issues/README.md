# Проектные документы Release Orchestrator

Здесь лежит только **незакрытое**. Документы, описывавшие уже построенное, удалены 2026-08-07 —
что именно было сделано и почему, стоит смотреть в истории коммитов и в комментариях самого кода,
а не в папке проектных заметок. Папка, полная выполненных планов, перестаёт читаться.

## Документы

| Документ | О чём | Что осталось |
|---|---|---|
| [006-per-task-planning.md](006-per-task-planning.md) | Планирование под задачу | `validate` и `import` плана задачи; тест «одна деривация»; удалить мёртвый `IntraTaskOrder` |
| [011-release-audit.md](011-release-audit.md) | Аудит перед релизом | Три незакрытых пункта: пересчёт в `GetActivePlanAsync`, точность `Version`, архив не забирает выкаченные задачи |
| [012-ordering-rules.md](012-ordering-rules.md) | Язык правил порядка выкатки (YAML) | Визуальный редактор. Сам язык, разбор и применение — сделаны; документ остаётся справочником по схеме |

## Что было удалено и почему

Девять документов, описывавших работу, которая закончена: аудит состояния до пивота и исследование
независимости от провайдеров (001, 002), устаревшая дорожная карта (003), целевая архитектура и
четыре её под-документа (004, 005, 007, 008, 009), приём вебхуков силами провайдера (010).

Всё, что они проектировали, стоит в коде: четыре оси расширения через keyed services, движок
выкатки с заявками на пару (MR, окружение), симметрия push/poll с inbox дедупликации, разбор
вебхуков на стороне провайдера, демонтаж глобального плана и стеков.

Ссылки на удалённые документы из комментариев кода сняты в том же коммите. Сами комментарии
самодостаточны — ссылка везде была хвостовой, и обоснование остаётся в них.
