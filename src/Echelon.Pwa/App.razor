<FlareThemeProvider>
    @* Cascades itself as the confirm-dialog host, so PageBase.ConfirmAsync can resolve it. *@
    <FlareConfirmDialogProvider>
        <CascadingAuthenticationState>
            <Router AppAssembly="@typeof(App).Assembly">
                <Found Context="routeData">
                    <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)">
                        <NotAuthorized>
                            <RedirectToLogin />
                        </NotAuthorized>
                    </AuthorizeRouteView>
                    <FocusOnNavigate RouteData="@routeData" Selector="h1" />
                </Found>
                <NotFound>
                    <AppTitle Title="@UiStrings.NotFound_Title" />
                    <LayoutView Layout="@typeof(MainLayout)">
                        <FlareAlert Severity="AlertSeverity.Warning">@UiStrings.NotFound_Message</FlareAlert>
                    </LayoutView>
                </NotFound>
            </Router>
        </CascadingAuthenticationState>
    </FlareConfirmDialogProvider>
</FlareThemeProvider>
